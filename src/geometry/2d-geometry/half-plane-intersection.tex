\begin{enumerate}
\item \texttt{cut}: Online in $O(n^2)$.
\item \texttt{half\_plane\_intersect}: Offline in $O(mlogm)$.
\end{enumerate}
\lstinputlisting{src/geometry/2d-geometry/half-plane-intersection.cpp}
