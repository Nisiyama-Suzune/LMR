Find the immediate dominator (\texttt{idom[]}) of each node, \texttt{idom[x]} will be $x$ if $x$ does not have a dominator, and will be $-1$ if $x$ is not reachable from $s$. 
\lstinputlisting{src/graph/cut/dominator-tree.cpp}

